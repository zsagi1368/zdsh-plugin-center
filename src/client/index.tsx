/**
 * zdsh-plugin-center — client-side entry (placeholder).
 *
 * The full marketplace UI (M3) replaces this file. It keeps the build gate
 * green and pins the two constants every other layer agrees on.
 */
export const PLUGIN_CENTER_SLOT_ID = 'zdsh-plugin-center';
export const PLUGIN_CENTER_SLOT_ORDER = 30;

export default function PluginCenterPlaceholder() {
  return null;
}
